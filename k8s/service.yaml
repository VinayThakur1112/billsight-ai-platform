apiVersion: v1
kind: Service
metadata:
  name: ingestion-svc
  annotations:
    networking.gke.io/load-balancer-type: "Internal"
spec:
  selector:
    app: ingestion
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer